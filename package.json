{
  "name": "valachchenai-harbor-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npx prisma generate",
    "start": "node server/src/server.js"
  }
}
